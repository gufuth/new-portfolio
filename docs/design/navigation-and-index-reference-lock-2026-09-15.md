# Navigation + index reference lock — 2026-09-15

**Status:** active navigation principle. Exact visual implementation for Work 1 → Work 2 remains to be prototyped and user-approved.

## User-selected references

### Fantastic Man
Approved borrow for case-study pages:
- editorial authority: one dominant project image + decisive type hierarchy;
- INDEX as a real alternate mode;
- short, scannable entries for rapid retrieval.

Use structurally, not as a literal visual copy. Keep Last Stop's dark evidence-cut shell, palette firewall, asymmetry, negative space, and native client color.

### Le Cinéma Club
Approved borrow for navigation / retrieval:
- a plain, direct index/list layer that lets a recruiter jump straight to a project;
- this is a fast route through the portfolio, not the primary cinematic route.

## Navigation architecture

Treat the site as having two navigation speeds.

### 1. Cinematic / spatial route
This is the primary authored experience.

Landing → Work 1 → Work 2 should feel like movement through the same diner world, not switching tabs in a portfolio UI.

For Work 1 → Work 2:
- preserve the physical idea that the viewer moves to another booth/window bay;
- use one restrained continuity edit from the existing transition family;
- keep top/bottom rails stable enough to maintain orientation;
- a subordinate in-world directional cue is allowed if it feels like cheap pre-existing property signage;
- do not make a giant button, pagination dots, carousel arrows, floating pills, or a modern product UI control;
- do not require discovery of the in-world cue for accessibility; a plain semantic route must remain available.

### 2. Fast / retrieval route
A quiet **INDEX / ALL WORK** control gives direct access to every project.

- This is not a second aesthetic world.
- It can be extremely plain.
- Client/project should be readable immediately.
- Optional tiny descriptor only when genuinely useful.
- No thumbnail-card dashboard by default.
- It should let a recruiter bypass the spatial narrative without punishing them.

## Current best working recommendation

For desktop Work 1 and Work 2, use **both**:

1. a subtle spatial NEXT BAY / MORE WORK cue integrated into the diner architecture or persistent rail, and
2. a quiet INDEX / ALL WORK escape hatch that opens the direct project list.

These solve different problems and should not be collapsed into one control.

## Governing principle

**Stay in the film when moving through the experience. Step outside the film only when the user explicitly asks for speed.**

## Drive record

The user-sent annotated Fantastic Man and Le Cinéma Club screenshot references, plus their attached decisions, are embedded in:

`00 — CASE STUDY VISUAL AUDIT + PORTFOLIO FINISH ROADMAP — 2026-09-08`

Google Drive document ID: `1t-lJlr3JCSft3_hKuTh2pX7VGPBgysFqckk29cjhBX4`
